{
  "name": "vite-project",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "cross-env NODE_ENV=development vite preview",
    "dev": "cross-env NODE_ENV=development vite build",
    "start:service": "npm run build && npm run preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "sass": "^1.61.0",
    "vite": "^6.2.0",
    "vite-plugin-pug-i18n": "^2.0.0"
  }
}
